# 第 7 章 指令系统

## 例题部分

（无）

## 练习题部分

### 7.1：什么叫机器指令，什么叫指令系统？为什么说指令系统与机器的主要功能以及硬件结构之间存在着密切的关系？

1. 机器语言是由一条条语句构成的，每一条语句又能准确表达某种语义。人们习惯把**每一条机器语言的语句称为机器指令**，而将**全部机器指令的集合称为机器的指令系统**。计算机就是连续执行每一条机器语句而实现全自动工作的。
2. 计算机**设计者主要研究如何确定机器的指令系统，如何用硬件电路、芯片、设备来实现机器指令系统的功能**。计算机的**使用者则是依据机器提供的指令系统，使用汇编语言来编制各种程序**。计算机**使用者根据机器指令系统所描述的机器功能，能很清楚地了解计算机内部寄存器-存储器的内部结构，以及计算机能直接支持的各种数据类型**，所以说指令系统与机器的主要功能以及硬件结构之间存在着密切的关系。

### 7.3：什么是指令字长、机器字长和存储字长？

- **指令字长**是指**机器指令中二进制代码的总位数**。
- **机器字长**是指 **CPU 一次能处理数据的位数**，通常与 CPU 的寄存器位数有关。
- **存储字长**是指**存储单元中存放二进制代码的总位数**。

三者可以相等也可以不等视不同机器而定。

### 7.4：零地址指令的操作数来自哪里？在一地址指令中，另一个操作数的地址通常可采取什么寻址方式获得？各举一例说明。

零地址指令在指令字中**无地址码**。例如：空操作（NOP）、停机（HLT）这类指令只有操作码而**无操作数地址**；而子程序返回（RET）、中断返回（IRET）这类指令虽然没有显式地址码，但其**操作数地址**通常**隐含在堆栈指针 $\text{SP}$ 中**。

::: info 堆栈指针 $\text{SP}$

堆栈指针 $\text{SP}$ 是一个专用寄存器，用来指示**栈顶单元的地址**。程序执行压栈、出栈、调用子程序、返回等操作时，机器都要依据 $\text{SP}$ 来访问栈顶数据，并自动修改其内容。

因此，像 `RET`、`IRET` 这类零地址指令虽然在指令字中没有给出显式地址，但它们要取出的返回地址或现场信息实际上都**隐含在 $\text{SP}$ 所指向的栈顶位置中**。

:::

在一地址指令中，它的地址码字段只有一个，其格式如下：

$$
\begin{array}{|c|c|}
\hline
\mathrm{OP} & A_1 \\
\hline
\end{array}
$$

它可完成 $(\mathrm{ACC})\ \mathrm{OP}\ (A_1)\rightarrow \mathrm{ACC}$ 的操作，即 $\mathrm{ACC}$ 中原有内容作为一个操作数参与运算，运算结果仍送回 $\mathrm{ACC}$。因此，在一地址指令中，另一个操作数地址通常采用**隐含寻址方式**获得，典型例子是**累加器 $\mathrm{ACC}$**。

::: info <span id="寻址方式">寻址方式</span>

寻址方式是指**在执行指令时，确定操作数地址或直接得到操作数的方法**。也就是说，指令中的地址字段不一定就是操作数的实际地址，机器还需要按规定的方式形成有效地址，或者直接从某个约定位置取得操作数。

常见的寻址方式主要有以下几种：

- **隐含寻址**：操作数地址不在指令中给出，而由指令系统事先约定。例如一地址指令中，另一个操作数默认在累加器 $\mathrm{ACC}$ 中；`RET` 指令中的返回地址则隐含在堆栈指针 $\text{SP}$ 所指示的栈顶位置。
- **立即寻址**：操作数本身就直接写在指令字中，取指后即可得到操作数，不必再访问主存形成有效地址。
- **直接寻址**：指令中的地址字段就是操作数的有效地址，即 $EA=A$。
- **间接寻址**：指令中的地址字段先给出一个存储单元地址，该单元中存放的内容才是有效地址，因此需先访存再取得操作数地址。
- **寄存器寻址**：地址字段给出某个寄存器编号，操作数直接存放在该寄存器中。
- **寄存器间接寻址**：地址字段给出某个寄存器编号，但寄存器中存放的不是操作数本身，而是操作数在主存中的有效地址。
- **基址寻址**：有效地址由基址寄存器内容与指令中的位移量相加得到，即 $EA=(BR)+A$，常用于程序重定位。
- **变址寻址**：有效地址由变址寄存器内容与形式地址相加得到，即 $EA=A+(IX)$，常用于数组、表格等连续数据处理。
- **相对寻址**：有效地址由程序计数器 $\mathrm{PC}$ 的内容与位移量相加得到，即 $EA=(PC)+A$，常用于转移指令和程序浮动。

本题中的一地址指令，地址字段只给出一个操作数地址；另一个操作数通常不必再由地址码指出，而是默认放在累加器 $\mathrm{ACC}$ 中，因此这里用到的就是**隐含寻址**。

:::

### 7.5：对于二地址而言，操作数的物理地址可安排在什么地方？举例说明。

二地址指令中只含有两个地址字段，其格式如下：

$$
\begin{array}{|c|c|c|}
\hline
\mathrm{OP} & A_1 & A_2 \\
\hline
\end{array}
$$

它可完成 $(A_1)\ \mathrm{OP}\ (A_2)\rightarrow A_1$ 的操作，即 $A_1$ 字段既代表源操作数的地址，又代表本次运算结果的存放地址；有的机器也可表示 $(A_1)\ \mathrm{OP}\ (A_2)\rightarrow A_2$，此时 $A_2$ 除了代表源操作数的地址外，还代表运算结果的存放地址。这两种情况下，完成一条指令通常都需 3 次访存，其含义是中间结果暂存于累加器 $\mathrm{ACC}$ 中。

因此，对于二地址指令而言，操作数的物理地址可安排在**寄存器**（寄存器寻址）内，**指令**中（直接寻址）或**内存单元**（间接寻址）中。

### 7.6：某指令系统字长为 16 位，地址码取 4 位，试提出一种方案，使指令系统有 8 条三地址指令、16 条二地址指令、100 条一地址指令。

三地址码格式如下：

$$
\begin{array}{|c|c|c|c|}
\hline
\mathrm{OP}(4\text{ 位}) & A_1(4\text{ 位}) & A_2(4\text{ 位}) & A_3(4\text{ 位}) \\
\hline
\end{array}
$$

采用扩展操作码技术进行设计，指令操作码分配方案如下：

$$
\begin{array}{l}

\left.
\begin{array}{l}
0000,\\
\cdots,\\
0111,
\end{array}
\right\}
\quad A_1,A_2,A_3 & 8\text{ 条三地址指令} \\[24pt]

\left.
\begin{array}{l}
1000,\,0000,\\
\cdots,\,\cdots,\\
1000,\,1111
\end{array}
\right\}
\quad A_2,A_3 & 16\text{ 条二地址指令} \\[24pt]

\left.
\begin{array}{l}
1001,\,0000,\,0000,\\
\cdots,\,\cdots,\,\cdots,\\
1001,\,0110,\,0011,
\end{array}
\right\}
\quad A_3 & 100\text{ 条一地址指令} \\[24pt]

\left.
\begin{array}{l}
1001,\,0110,\,0100,\\
\cdots,\,\cdots,\,\cdots,\\
1001,\,1111,\,1111
\end{array}
\right\}
\quad \text{冗余编码} & \text{可用来扩充二、零地址指令} \\[24pt]

\left.
\begin{array}{l}
1010,\\
\cdots,\\
1111,
\end{array}
\right\}
\quad \text{冗余编码} & \text{可用来扩充三、二、一、零地址指令}

\end{array}
$$

### 7.7：设指令字长为 16 位，采用扩展操作码技术，每个操作码的地址为 6 位。如果定义了 13 条二地址指令，试问还可安排多少条一地址指令？

二地址指令格式如下：

$$
\begin{array}{|c|c|c|}
\hline
\mathrm{OP}(4\text{ 位}) & A_1(6\text{ 位}) & A_2(6\text{ 位}) \\
\hline
\end{array}
$$

设二地址指令格式为该指令系统的基本格式，**4 位**操作码共有 **16 种**编码，其中 **13 种**用来定义二地址指令，还剩 **3 种**可用作扩展标志。如不考虑地址指令，该指令系统最多还能安排：

$$
\text{一地址指令条数}=3\times 2^6=192\text{ 条}
$$

### 7.8：某机指令字长 16 位，每个操作数的地址码为 6 位，设操作码长度固定，指令分为零地址、一地址和二地址三种格式，若零地址指令有 $M$ 种，一地址指令有 $N$ 种，则二地址指令最多有几种？若操作码位数可变，则二地址指令最多允许几种？

::: details 定长操作码和变长操作码

操作码是用来指出**指令要完成哪一种操作**的编码字段。按操作码长度是否固定，可分为**定长操作码**和**变长操作码**两类。

- **定长操作码**：所有指令的操作码位数都相同。这样做的优点是**指令译码简单、硬件实现方便**；缺点是**编码利用率不够高**，因为地址数较少的指令虽然本可腾出更多位给操作码，但在定长方案下不能灵活利用。
- **变长操作码**：不同类型指令可采用不同位数的操作码。通常地址字段越少，腾出来的位越多，就可以分配给更长的操作码，从而定义更多指令。它的优点是**编码更紧凑、指令条数安排更灵活**；缺点是**译码较复杂**，硬件设计也更麻烦。

定长操作码下，各类指令共用同样长的操作码字段；变长操作码下，则可以利用地址码减少后空出的位，继续扩展操作码。

:::

1. 采用**定长操作码**时，二地址指令格式如下：

   $$
   \begin{array}{|c|c|c|}
   \hline
   \mathrm{OP}(4\text{ 位}) & A_1(6\text{ 位}) & A_2(6\text{ 位}) \\
   \hline
   \end{array}
   $$

   此时，无论指令中有几个地址，指令格式都不变。

   设二地址指令有 $K$ 种，则：

   $$
   K=2^4-M-N
   $$

   当 $M=1$（最小值），$N=1$（最小值）时，二地址指令最多有：

   $$
   K_{\max}=16-1-1=14\text{ 种}
   $$

2. 若采用**变长操作码**时，二地址指令格式仍为上式所示，但操作码长度可随地址码个数而变。此时：

   $$
   K=2^4-\left(\frac{N}{2^6}+\frac{M}{2^{12}}\right)
   $$

   当 $\left(\dfrac{N}{2^6}+\dfrac{M}{2^{12}}\right)\leq 1$ 时，$\left(\dfrac{N}{2^6}+\dfrac{M}{2^{12}}\right)$ 向上取整后最小，故 $K$ 最大，则二地址指令最多有：

   $$
   K_{\max}=16-1=15\text{ 种}
   $$

   ::: info 讨论

   此时，一地址码条数为：

   $$
   N=\left(2^4-K\right)\times 2^6-\frac{M}{2^6}
   $$

   其中，$\dfrac{N}{2^6}$ 取向上整值。

   零地址码条数为：

   $$
   M=2^{16}-2^{12}K-2^6N
   $$

   当 $K$ 最大（$K=15$）时，一地址指令最多有：

   $$
   N_{\max}=64-1=63\text{ 种}
   $$

   零地址指令最多有：

   $$
   M_{\max}=64\text{ 种}
   $$

   :::

### 7.9：试比较间接寻址和寄存器间接寻址。

间接寻址和寄存器间接寻址都可扩大寻址范围，但他们形成有效地址的方式不同。

- **间接寻址**：间接寻址**需通过访存**（若是多次间址还需多次访存）**得到有效地址**，同时间接寻址的第二个优点在于它便于编制程序。间接寻址的缺点在于指令的执行阶段需要访存两次（一次间接寻址）或多次（多次间接寻址），致使指令执行时间延长。
- **寄存器间接寻址**：寄存器间接寻址**需通过访问寄存器获得有效地址在内存中的地址**，**再通过访存获得**。因有效地址不是存放在存储单元中，而是存放在寄存器中，它比间接寻址少访存一次。故间址指令执行的时间比寄存器间接寻址长。

### 7.10：试比较基址寻址和变址寻址。

1. 都可**有效地扩大指令寻址范围**。
2. **基址寻址**时，**基准地址由基准寄存器给出**，地址的改变反映在位移量 $A$ 的取值上；**变址寻址**时，**基准地址由 $A$ 给出**，地址的改变在变址值的自动修改上，变址值由变址寄存器给出。
3. **基址寄存器**内容通常**由系统程序设定**，**变址寄存器**内容通常**由用户设定**。
4. **基址寻址**适用于**程序的动态重定位**，**变址寻址**适用于**数组或字符串处理**，适用场合不同。

### 7.11：画出先变址再间址寻址及先间址再变址寻址的过程示意图。

1. **先变址再间址的寻址过程**：

   ![先变址再间址的寻址过程](https://static.owo.cab/notes/image/cs/poco/chapter07/先变址再间址的寻址过程.webp)

2. **先间址再变址的寻址过程**：

   ![先间址再变址的寻址过程](https://static.owo.cab/notes/image/cs/poco/chapter07/先间址再变址的寻址过程.webp)

### 7.16：某机主存容量为 $4\,\text{M}\times 16$ 位，且存储字长等于指令字长，若该机指令系统可完成 $108$ 种操作，操作码位数固定，且具有直接、间接、变址、基址、相对、立即等六种[**寻址方式**](#寻址方式)，试回答以下问题。<ol><li>画出一地址指令格式并指出各字段的作用。</li><li>该指令直接寻址的最大范围。</li><li>一次间接寻址和多次间接寻址的寻址范围。</li><li>立即数的范围（十进制表示）。</li><li>相对寻址的位移量（十进制表示）。</li><li>上述六种寻址方式的指令中哪一种执行时间最短，哪一种最长，为什么？哪一种便于程序浮动，哪一种最适合处理数组问题？</li><li>如何修改指令格式，使指令的寻址范围可扩大到 $4\,\text{M}$？</li><li>为使一条转移指令能转移到主存的任一位置，可采取什么措施？简要说明之。</li></ol>

1. 单字长一地址指令格式：

   $$
   \begin{array}{|c|c|c|}
   \hline
   \mathrm{OP}(7\text{ 位}) & M(3\text{ 位}) & A(6\text{ 位}) \\
   \hline
   \end{array}
   $$

   $\text{OP}$ 为**操作码字段**，共 7 位，可反映 108 种操作；

   $\text{M}$ 为**寻址方式字段**，共 3 位，可反映 6 种寻址操作；

   $\text{A}$ 为**地址字段**，共 $16-7-3=6$ 位。

2. $\text{A}$ 为 $6$ 位，指令直接寻址的**最大范围为 $2^6=64$ 字**。

3. 由于存储字长为 $16$ 位，故**一次间址的寻址范围为 $2^{16}=64\,\text{K}$ 字**；若**多次间址**，需用存储字的最高位来区别是否继续间接寻址，故**寻址范围为 $2^{15}=32\,\text{K}$ 字**。

4. **立即数寻址范围**：若采用补码表示为 $1\text{F}\,\text{H}\sim 20\,\text{H}$；十进制表示为 $-32\sim 31$（有符号数），或 $0\sim 63$（无符号）。

5. **相对寻址的位移量范围**采用补码表示时同立即数范围，**为 $-32\sim 31$**。

6. 上述六种寻址方式中，

   因立即数由指令中直接给出，故**立即寻址的指令执行时间最短**。

   **间接寻址**在指令的执行阶段需多次访存（一次间接寻址要两次访存，多次间接寻址要多次访存），故**执行时间最长**。

   **相对寻址**操作数位置可随程序存储区的变动而改变，操作数的有效地址只与当前指令地址相差一定的位移量，与直接寻址相比，**更有利于程序浮动**。

   **变址寻址**由于变址寄存器的内容**由用户给定**，而且在程序的执行过程中允许用户修改，而其形式地址始终不变，（变址值可自动修改而不需要修改程序），故**变址寻址的指令便于用户编制处理数组问题的程序**。

7. <span id="7.16.7"></span>
   - **方案一**：为使指令寻址范围可扩大到 $4\,\text{M}$，需要有效地址 $22$ 位，此时可将单字长一地址的格式改为双字长，如下图所示：

     <table style="border-collapse: collapse; text-align: center; margin: 0.5em auto;">
       <tbody>
         <tr>
           <td style="border: 1px solid #999; padding: 0.35em 1.2em;"><InlineMath formula="\mathrm{OP}(7\text{ 位})" /></td>
           <td style="border: 1px solid #999; padding: 0.35em 1.2em;"><InlineMath formula="\mathrm{MOD}(3\text{ 位})" /></td>
           <td style="border: 1px solid #999; padding: 0.35em 1.2em;"><InlineMath formula="A(\text{高 }6\text{ 位})" /></td>
         </tr>
         <tr>
           <td colspan="3" style="border: 1px solid #999; padding: 0.35em 1.2em;"><InlineMath formula="A(\text{低 }16\text{ 位})" /></td>
         </tr>
       </tbody>
     </table>

     图中，指令的第一字保持原来格式不变，形式地址 $\text{A}$ 扩展到第 2 个字，这样，直接寻址时，$EA=A=16+6=22$ 位，正好可访问 $4\,\text{M}$ 地址空间。由于 $\text{A}$ 的扩展，变址、基址、相对、立即数等寻址方式也扩展到 $22$ 位。

   - **方案二**：如果仍采用单字长指令（16 位）格式，为使指令寻址范围可扩大到 $4\,\text{M}$，可通过段寻址方案实现。安排如下：

     硬件设段寄存器 $\text{DS}$（16 位），用来存放段地址。在完成指令寻址方式所规定的寻址操作后，得有效地址 $EA$（6 位），再由硬件自动完成段寻址，最后得 $22$ 位物理地址。即：物理地址 $=(\text{DS})\times 2^6+\text{EA}$

     注：段寻址方式由硬件隐含实现。在编程指定的寻址过程完成，$\text{EA}$ 产生之后由硬件自动完成，对用户是透明的。

   - **方案三**：在采用单字长指令（16 位）格式时，还可通过页面寻址方案使指令寻址范围扩大到 $4\,\text{M}$。安排如下：

     硬件设页面寄存器 $\text{PR}$（16 位），用来存放页面地址。指令寻址方式中增设页面寻址。当需要使指令寻址范围扩大到 $4\,\text{M}$ 时，编程选择页面寻址方式，则：$\text{EA}=\text{(PR)}|| \text{A}$（有效地址 = 页面地址“拼接”6 位形式地址），这样得 22 位有效地址。

8. 为使一条转移指令能转移到主存的任一位置，寻址范围达到 $4\,\text{M}$，除了**采用 [7.](#7.16.7) 方案一中的双字长一地址指令的格式**外，还可**配置 22 位的基址寄存器或 22 位的变址寄存器**，使 $\text{EA}=(\text{BR})+\text{A}$（$\text{BR}$ 为 22 位的基址寄存器）或 $\text{EA}=(\text{IX})+\text{A}$（$\text{IX}$ 为 22 位的基址寄存器），便可访问 $4\,\text{M}$ 存储空间。还可以**通过 16 位的基址寄存器左移 6 位再和形式地址 $\text{A}$ 相加**，也可达到同样的效果。

   总之，不论采取何种形式，最终得到的实际地址应是 $22$ 位。

### 7.18：某机器共能完成 78 种操作，若指令字长为 16 位，试问一地址格式的指令地址码可取几位？若想使指令寻址范围扩大到 $2^{16}$，可采用什么方法？举出三种不同例子加以说明。

因为机器共能完成 $2^7>78>2^6$ 种操作，所以一地址指令的操作码至少取 7 位。指令字长为 16 位，故指令地址码可取 $16-7=9$ 位。若想使指令寻址范围扩大到 $2^{16}$，可采用**寄存器间接寻址方式**、**间接寻址方式**或**变址寻址**。

1. **寄存器间接寻址方式**：

   ![寄存器间接寻址方式示意图](https://static.owo.cab/notes/image/cs/poco/chapter07/寄存器间接寻址方式示意图.drawio.svg)

   所以它的最大指令寻址范围可以是 $2^{9-1}\times 2^{16}=2^{24}>2^{16}$，故寄存器间接寻址方案可行。

2. **间接寻址方式**：

   ![间接寻址方式示意图](https://static.owo.cab/notes/image/cs/poco/chapter07/间接寻址方式示意图.drawio.svg)

   所以它的最大指令寻址范围可以是 $2^{9-1}\times 2^{16}=2^{24}>2^{16}$，故间接寻址方案可行。

3. **变址寻址方式**：

   ![变址寻址方式示意图](https://static.owo.cab/notes/image/cs/poco/chapter07/变址寻址方式示意图.drawio.svg)

   显然，变址寻址的寻址范围也符合要求。

### 7.20：什么是 RISC？简述它的主要特点。

**RISC** 即**精简指令系统计算机**（Reduced Instruction Set Computer），与之对应的是 CISC，即**复杂指令系统计算机**（Complex Instruction Set Computer）。人们从 80-20 规律中得到启示：仅用最常用的 20% 的简单指令，重新组合去实现不常用的 80% 的指令功能，这便引发出 RISC 技术。所以，RISC 指令系统就是通过简化指令，使复杂指令的功能由执行频度较高的简单指令组合来实现。

**RISC 的主要特点**：

1. 选取使用频度较高的一些简单指令以及一些很有用但又不复杂的指令，让复杂指令的功能由频度高的简单指令的组合来实现；
2. **指令长度固定**，**指令格式种类少**，**寻址方式种类少**；
3. 只有**取数/存数**（LOAD/STORE）指令访问存储器，其余指令的操作都在寄存器内完成；
4. CPU 中有**多个通用寄存器**；
5. 采用**流水线技术**，大部分指令在一个时钟周期内完成。采用超标量和超流水线技术，可使每条指令的平均执行时间小于一个时钟周期；
6. 控制器采用**组合逻辑控制**，不用微程序控制；
7. 采用**优化的编译程序**。

### 7.21：比较 RISC 和 CISC 的异同之处。

- **CISC** 体系结构的设计策略是**使用大量的指令**，包括复杂指令，每一项简单或复杂的任务都有一条对应的指令，程序员不需要写一大堆指令去完成一项复杂的任务。复杂的指令被转化为一系列简单操作然后由 CPU 执行。
- **RISC** 体系结构的设计策略是**使用少量的指令**完成最常用的简单操作，复杂指令则用简单指令子集来模拟，因此在 RISC 中进行程序设计比在其他设计中更难、更费时，因为复杂指令都用简单指令来模拟。

### 7.22：RISC 机中指令简单，有些常用的指令未被选用，它用什么方式来实现这些常用指令的功能。试举例说明。

RISC 机不仅精简了指令系统，采用超标量和超流水线结构；它们的指令数目只有几十条，却大大增强了并行处理能力。

RISC 机中指令简单，有些常用的指令未被选用，它通常**选取使用频度较高的一些功能简单、能在一个节拍内执行完成的指令**来实现这些常用指令的功能，而把较复杂的功能用**频度高的简单指令组成一段子程序**来实现。

例如，RISC II 共有 39 条指令，可分为以下 4 类：

1. **寄存器-寄存器操作**：移位、逻辑、算术（整数）运算等 12 条。
2. **取存数据指令**：取存字节、半字、字等 16 条。
3. **控制转移指令**：条件转移、调用/返回等 6 条。
4. **其他指令**：存取程序状态字 $\text{PSW}$ 和程序计数器等 5 条。

一些常用指令未被选中，但可在**硬件系统的辅助**下，足以实现其他一些指令的功能。例如：该机约定 $R_0$ 寄存器的内容恒为 0。这样，加法指令可替代寄存器间的传递指令，即 $(R_1)+(R_0)\rightarrow R_4$ 替代 $R_1\rightarrow R_4$。

加法指令还可以替代清零寄存器指令，即 $(R_0)+(R_0)\rightarrow R_4$ 替代了 $0\rightarrow R_4$。

减法指令可替代取负数指令，即 $(R_0)-(R_1)\rightarrow R_4$ 替代了将 $R_1$ 寄存器内容取负后送入 $R_4$。
